/**
 * _isdigit - checks for a digit (0 through 9)
 * Return: 0
 * @c: variable to be compared
 */
int _isdigit(int c)
{
	if ((c >= 0) && (c <= 9))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
int main(void)
{
	return (2);
}
